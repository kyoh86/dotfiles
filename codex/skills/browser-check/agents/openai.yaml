interface:
  display_name: "Browser Check"
  short_description: "Check localhost pages through Edge DevTools"
  default_prompt: "Use this skill to verify a localhost page by opening it in a browser context, observing the rendered result, and reporting what is actually visible and what fails."
