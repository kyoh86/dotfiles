set-option -g mode-keys vi

# Jump between shell prompts recorded by OSC 133 markers.
bind-key -T copy-mode-vi Up send-keys -X previous-prompt
bind-key -T copy-mode-vi Down send-keys -X next-prompt

# vim:ft=tmux
